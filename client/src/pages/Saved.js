import React, { Component } from "react";
import SavedCard from "../components/savedCard/savedCard";

class Saved extends Component {
    state = {
        books: []
    }

    //insert necessary methods

    render() {
        return (
            <div>
                <SavedCard 
                
                />
            </div>
        )
    }
}

export default Saved;